/** Declara una variable para representar la edad de un usuario. Luego, utiliza condicionales para determinar si el usuario es mayor o menor de edad 
 * y muestra un mensaje apropiado. */

var edadUsuario = 37;

if (edadUsuario >= 18) {
    console.log( "El usuario es mayor de edad." );
} else {
    console.log("El usuario es menor de edad.");
}
